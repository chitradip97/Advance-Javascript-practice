// Custom Condition:

// Write a function that uses the every function to check if all elements in an array are even.
const numbers = [10, 30, 50, 170, 90];
let checknum=numbers.every(num=>num%2==0);
console.log("output :"+checknum);