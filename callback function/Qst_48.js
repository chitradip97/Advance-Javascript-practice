// Use the some function to find if an array contains an element equal to a specific value.

const numbers = [10, 30, 50, 170, 90];
verify=numbers.some(number=>number==50);
console.log("Output :"+verify);